flask
ultralytics>=8.0.0
# ติดตั้ง torch/torchvision แบบ CPU-only ใน Dockerfile แทนการใส่ตรงนี้
opencv-python-headless>=4.8.0
Pillow>=9.5.0
numpy>=1.24.0
# pymongo>=4.6.0   # ถ้าใช้ MongoDB ให้ uncomment
