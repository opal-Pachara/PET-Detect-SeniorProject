# Simple RFID + Camera + API System Requirements

# Core libraries (จาก code เดิม)
opencv-python>=4.8.0
requests>=2.28.0

# RFID library (เพิ่มใหม่)
mfrc522>=1.0.0

# Additional utilities
numpy>=1.24.0 